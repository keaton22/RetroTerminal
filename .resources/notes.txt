NOTES:

- result has a 5000ms timer before it expires
- existing result can be overwritten by new result before expiration

- <a> elements can't have result with current structure

- click link => go to new view => see result on new page
- click button => do action (reload current page) => see result on current page
- result:
	- to indicate completed/pending actions, use ...
	- to indicate information, DON'T use ...